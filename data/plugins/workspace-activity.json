{
  "id": "workspace-activity",
  "repo": "ABorakati/paseo-workspace-activity",
  "url": "https://github.com/ABorakati/paseo-workspace-activity",
  "name": "workspace-activity",
  "description": "Paseo plugin providing workspace Tasks and Agent Monitor panels",
  "version": "0.1.0",
  "author": "Aditya Borakati",
  "license": "MIT",
  "categories": [
    "monitoring",
    "productivity"
  ],
  "platforms": [],
  "caveats": [],
  "paseoVersionRequirement": ">=0.8.0",
  "manifest": {
    "id": "workspace-activity",
    "requirements": {
      "paseo": ">=0.8.0"
    }
  },
  "repoMeta": {
    "stars": 0,
    "openIssues": 1,
    "defaultBranch": "main",
    "pushedAt": "2026-09-08T10:26:01Z",
    "topics": [],
    "archived": false,
    "license": "MIT"
  },
  "owner": {
    "login": "ABorakati",
    "avatarUrl": "https://avatars.githubusercontent.com/u/41518783?v=4",
    "url": "https://github.com/ABorakati"
  },
  "installNotes": "> **Requires Paseo v0.8.0 or newer.**\n\nPlugin code is trusted and unsandboxed. The server half runs in a subprocess with full access to the daemon machine. Read a plugin before you install it.\n\nMake sure plugins are enabled first: **Settings > Plugins > Enable plugins** in the app, or `pluginsEnabled: true` in the daemon config. Then install straight from Git:\n\n```bash\npaseo plugin add ABorakati/paseo-workspace-activity\n```\n\nThat clones the repository, compiles it on the daemon, and reaches **running** in `paseo plugin ls` with no package manager and no install scripts. Git installs track `main`; `paseo plugin status` shows when the upstream repository has moved and `paseo plugin update workspace-activity` pulls it.\n\n### Find it\n\nThis plugin contributes workspace panels, not sidebar entries, because both views are scoped to the agents of one workspace. Open any workspace, then:\n\n- **Workspace tab** — the tab picker lists **Agent Monitor** and **Tasks** alongside Terminal, Files and the other built-in tabs. Each opens as a full tab in the workspace.\n- **Explorer panel** — the same two panels can be docked into the **Explorer** side panel so they stay visible beside an agent conversation.\n- **Command palette** — `Open Agent Monitor`, `Open tasks`, and the `... in Explorer` variants of each.\n- **Composer slash commands** — type `/tasks` or `/agents` in any workspace composer to open the matching panel without leaving your chat.\n\nBoth panels are empty until at least one agent has…",
  "installNotesHtml": "<blockquote>\n<p><strong>Requires Paseo v0.8.0 or newer.</strong></p>\n</blockquote>\n<p>Plugin code is trusted and unsandboxed. The server half runs in a subprocess with full access to the daemon machine. Read a plugin before you install it.</p>\n<p>Make sure plugins are enabled first: <strong>Settings > Plugins > Enable plugins</strong> in the app, or <code>pluginsEnabled: true</code> in the daemon config. Then install straight from Git:</p>\n<pre><code class=\"language-bash\">paseo plugin add ABorakati/paseo-workspace-activity\n</code></pre>\n<p>That clones the repository, compiles it on the daemon, and reaches <strong>running</strong> in <code>paseo plugin ls</code> with no package manager and no install scripts. Git installs track <code>main</code>; <code>paseo plugin status</code> shows when the upstream repository has moved and <code>paseo plugin update workspace-activity</code> pulls it.</p>\n<h3>Find it</h3>\n<p>This plugin contributes workspace panels, not sidebar entries, because both views are scoped to the agents of one workspace. Open any workspace, then:</p>\n<ul>\n<li><strong>Workspace tab</strong> — the tab picker lists <strong>Agent Monitor</strong> and <strong>Tasks</strong> alongside Terminal, Files and the other built-in tabs. Each opens as a full tab in the workspace.</li>\n<li><strong>Explorer panel</strong> — the same two panels can be docked into the <strong>Explorer</strong> side panel so they stay visible beside an agent conversation.</li>\n<li><strong>Command palette</strong> — <code>Open Agent Monitor</code>, <code>Open tasks</code>, and the <code>... in Explorer</code> variants of each.</li>\n<li><strong>Composer slash commands</strong> — type <code>/tasks</code> or <code>/agents</code> in any workspace composer to open the matching panel without leaving your chat.</li>\n</ul>\n<p>Both panels are empty until at least one agent has…</p>",
  "health": {
    "manifestValid": true,
    "hasReadme": true,
    "hasLicense": true,
    "hasTests": true,
    "hasTypecheckScript": true,
    "updatedRecently": true
  },
  "images": [
    "https://raw.githubusercontent.com/ABorakati/paseo-workspace-activity/main/docs/images/agent-monitor.png",
    "https://raw.githubusercontent.com/ABorakati/paseo-workspace-activity/main/docs/images/tasks.png"
  ],
  "videos": [],
  "scannedAt": "2026-09-08T13:18:55.518Z"
}
