{
  "id": "launchd-jobs",
  "repo": "gpambrozio/paseo-plugins",
  "path": "launchd-jobs",
  "url": "https://github.com/gpambrozio/paseo-plugins/tree/main/launchd-jobs",
  "name": "paseo-launchd-jobs",
  "description": "A Paseo plugin that schedules shell commands on your Mac through launchd. It adds a **Scheduled jobs** sidebar surface where you create a job — a name, a command, a working directory, and either a cron expression or a fixed interval — and the plugin writes it as a LaunchAgent. From then on launchd runs it, whether or not Paseo is open. The surface shows what launchd knows about each job, the last ",
  "version": "0.1.0",
  "license": "MIT",
  "categories": [
    "automation"
  ],
  "platforms": [
    "macos"
  ],
  "caveats": [
    "Requires an active login session — won't run from a headless SSH-only daemon"
  ],
  "manifest": {
    "id": "launchd-jobs"
  },
  "repoMeta": {
    "stars": 2,
    "openIssues": 0,
    "defaultBranch": "main",
    "pushedAt": "2026-09-08T01:09:51Z",
    "topics": [
      "developer-tools",
      "launchd",
      "macos",
      "paseo",
      "paseo-plugin",
      "plugin",
      "react-native",
      "typescript"
    ],
    "archived": false,
    "license": "MIT"
  },
  "owner": {
    "login": "gpambrozio",
    "avatarUrl": "https://avatars.githubusercontent.com/u/672135?v=4",
    "url": "https://github.com/gpambrozio"
  },
  "installNotes": "```bash\npaseo plugin add gpambrozio/paseo-plugins --path launchd-jobs\n```\n\nOr from a clone:\n\n```bash\ncd paseo-plugins/launchd-jobs\nnpm install\nnpm run typecheck\npaseo plugin install \"$PWD\"\n```\n\nThe **daemon** has to be running on macOS, because that is where the agents live and where the\ncommands run. A daemon on Linux loads the plugin and the surface says so instead of showing a list.\nThe Paseo app can be anywhere.",
  "installNotesHtml": "<pre><code class=\"language-bash\">paseo plugin add gpambrozio/paseo-plugins --path launchd-jobs\n</code></pre>\n<p>Or from a clone:</p>\n<pre><code class=\"language-bash\">cd paseo-plugins/launchd-jobs\nnpm install\nnpm run typecheck\npaseo plugin install \"$PWD\"\n</code></pre>\n<p>The <strong>daemon</strong> has to be running on macOS, because that is where the agents live and where the\ncommands run. A daemon on Linux loads the plugin and the surface says so instead of showing a list.\nThe Paseo app can be anywhere.</p>",
  "limitationsNotes": "- macOS only. A daemon on Linux would need a systemd user timer backend, which does not exist here.\n- The daemon has to run inside your login session — which it does when the desktop app starts it.\n  A daemon started over SSH with no GUI session may not be able to load agents into it.\n- Six-field cron expressions with seconds are refused; launchd has no seconds field. Use a fixed\n  interval instead.\n- What launchd reports — whether a job is running, its process id, its spawn count — comes from\n  `launchctl print`, whose output is prose. If a macOS release rewords it, those facts go blank\n  until the parser is updated; the run history and the log do not depend on it.",
  "limitationsNotesHtml": "<ul>\n<li>macOS only. A daemon on Linux would need a systemd user timer backend, which does not exist here.</li>\n<li>The daemon has to run inside your login session — which it does when the desktop app starts it.\nA daemon started over SSH with no GUI session may not be able to load agents into it.</li>\n<li>Six-field cron expressions with seconds are refused; launchd has no seconds field. Use a fixed\ninterval instead.</li>\n<li>What launchd reports — whether a job is running, its process id, its spawn count — comes from\n<code>launchctl print</code>, whose output is prose. If a macOS release rewords it, those facts go blank\nuntil the parser is updated; the run history and the log do not depend on it.</li>\n</ul>",
  "health": {
    "manifestValid": true,
    "hasReadme": true,
    "hasLicense": true,
    "hasTests": true,
    "hasTypecheckScript": true,
    "updatedRecently": true
  },
  "images": [
    "https://raw.githubusercontent.com/gpambrozio/paseo-plugins/main/launchd-jobs/docs/screenshot.png"
  ],
  "videos": [],
  "scannedAt": "2026-09-08T14:07:14.998Z"
}
